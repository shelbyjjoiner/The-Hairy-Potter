// Imports go first

import { makePottery } from "./PotteryWheel.js/potterywheel"



// Make 5 pieces of pottery at the wheel
let mug = makePottery()


// Fire each piece of pottery in the kiln


// Determine which ones should be sold, and their price


// Invoke the component function that renders the HTML list



///checking your work
//invoke the makePottery function and provide the values as arguments. 
//store that object that gets returned into a variable 
let checkingProg = makePottery(shapeParam, weightParam, heightParam)
console.log(checkingProg)